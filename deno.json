{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "run-once": "deno run -A main.ts",
    "build-types": "deno run -A scripts/createTypes.ts",
    "compile": "deno compile --unstable --allow-all main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
